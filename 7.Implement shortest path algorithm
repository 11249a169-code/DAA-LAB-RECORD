EXPNO:07
DATE:31|03|26

AIM:
To find shortest path using Dijkstra algorithm.

Program:
#include<stdio.h>
int main()
{
    int cost[10][10],dist[10],visited[10];
    int n,i,j,count=1,min,next,start;
    printf("Enter number of nodes: ");
    scanf("%d",&n);
    printf("Enter cost matrix:\n");
    for(i=0;i<n;i++)
        for(j=0;j<n;j++)
            scanf("%d",&cost[i][j]);
    printf("Enter start node: ");
    scanf("%d",&start);
    for(i=0;i<n;i++)
    {
        dist[i]=cost[start][i];
        visited[i]=0;
    }
    visited[start]=1;
    while(count<n-1)
    {
        min=999;
        for(i=0;i<n;i++)
        {
            if(dist[i]<min && !visited[i])
            {
                min=dist[i];
                next=i;
            }
        }
        visited[next]=1;
        for(i=0;i<n;i++)
        {
            if(!visited[i])
                if(min+cost[next][i]<dist[i])
                    dist[i]=min+cost[next][i];
        }
        count++;
    }
    for(i=0;i<n;i++)
        printf("\nDistance from source to %d = %d",i,dist[i]);
}

OUTPUT:
Enter source: 0
Distance to 0 = 0
Distance to 1 = 4
Distance to 2 = 6
Distance to 3 = 8
Distance to 4 = 7

RESULT:
Thus shortest path is found using Dijkstra algorithm.
